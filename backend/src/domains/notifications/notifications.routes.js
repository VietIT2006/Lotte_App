const express = require('express');
const notificationsController = require('./notifications.controller');

const router = express.Router();

// Example: router.get('/', notificationsController.getAll);

module.exports = router;