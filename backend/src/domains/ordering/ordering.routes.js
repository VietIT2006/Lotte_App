const express = require('express');
const orderingController = require('./ordering.controller');

const router = express.Router();

// Example: router.get('/', orderingController.getAll);

module.exports = router;