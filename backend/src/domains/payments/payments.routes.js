const express = require('express');
const paymentsController = require('./payments.controller');

const router = express.Router();

// Example: router.get('/', paymentsController.getAll);

module.exports = router;