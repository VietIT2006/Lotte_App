const express = require('express');
const usersController = require('./users.controller');

const router = express.Router();

// Example: router.get('/', usersController.getAll);

module.exports = router;