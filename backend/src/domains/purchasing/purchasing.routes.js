const express = require('express');
const purchasingController = require('./purchasing.controller');

const router = express.Router();

// Example: router.get('/', purchasingController.getAll);

module.exports = router;