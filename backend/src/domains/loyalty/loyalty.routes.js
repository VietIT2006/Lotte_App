const express = require('express');
const loyaltyController = require('./loyalty.controller');

const router = express.Router();

// Example: router.get('/', loyaltyController.getAll);

module.exports = router;