const express = require('express');
const inventoryController = require('./inventory.controller');

const router = express.Router();

// Example: router.get('/', inventoryController.getAll);

module.exports = router;